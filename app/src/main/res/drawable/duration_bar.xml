<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/material_gray_050"
                android:centerColor="@color/material_gray_200"
                android:centerY="1.0"
                android:endColor="@color/material_gray_300"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/material_blue_400"
                    android:centerColor="@color/material_blue_500"
                    android:centerY="1.0"
                    android:endColor="@color/material_blue_500"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>